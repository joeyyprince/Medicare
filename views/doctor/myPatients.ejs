<%- include('../partials/header') %>
<h2 style="margin-bottom:20px;">My Patients</h2>
<% if (patients.length === 0) { %>
  <p>No patients assigned to you yet.</p>
<% } else { %>
<table style="width:100%; border-collapse:collapse;">
  <thead>
    <tr style="background:#2c3e50; color:white;">
      <th style="padding:10px; text-align:left;">Name</th>
      <th style="padding:10px; text-align:left;">Email</th>
      <th style="padding:10px; text-align:left;">Phone</th>
      <th style="padding:10px; text-align:left;">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% patients.forEach(patient => { %>
    <tr style="border-bottom:1px solid #ddd;">
      <td style="padding:10px;"><%= patient.name %></td>
      <td style="padding:10px;"><%= patient.email %></td>
      <td style="padding:10px;"><%= patient.phone || 'N/A' %></td>
      <td style="padding:10px;">
        <a href="/doctor/patients/<%= patient._id %>" style="background:#2c3e50; color:white; padding:5px 10px; border-radius:4px; text-decoration:none;">View Details</a>
      </td>
    </tr>
    <% }) %>
  </tbody>
</table>
<% } %>
<%- include('../partials/footer') %>